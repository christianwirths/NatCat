name: NatCat
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pandas
  - numpy
  - scipy
  - requests
  - tqdm
  - jupyterlab
  - matplotlib
  - seaborn
  - cartopy
  # We will need these later for geospatial work:
  - geopandas 
  - shapely
